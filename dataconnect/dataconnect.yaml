specVersion: "v1"
serviceId: "bookstore"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bookstore-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
